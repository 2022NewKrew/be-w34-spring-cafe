spring:
  mustache:
    cache: false
    suffix: .html
    expose-session-attributes: true
  datasource:
    url: jdbc:mysql://db:3306/kakaodb
    username: root
    password: password
  mvc:
    hiddenmethod:
      filter:
        enabled: true
server:
  servlet:
    session:
      tracking-modes: cookie