{{>layout/head}}
{{>layout/header}}
{{#user}}
  <div class="profile-container">
    <div>{{userId.uuid}}</div>
    <div>{{nickname}}</div>
    <div>{{email}}</div>
  </div>
{{/user}}

<style>
  .profile-container {
    max-width: 1048px;
    margin: 100px auto;
  }
</style>

{{>layout/footer}}