<body>
{{>layout/header}}
<div class="users-container">
  <div class="users-title-box">
    <div class="users-title">멤버리스트</div>
    <div class="users-title" style="font-size: 24px">참여자를 확인할 수 있습니다.</div>
  </div>
  <div class="user-row">
    <span class="user-text-box">닉네임</span>
    <span class="user-text-box">이메일</span>
    <span class="user-text-box">회원가입일</span>
  </div>
  {{#users}}
    <div class="user-row">
      <span class="user-text-box click-box"
            onclick="location.href='/users/{{userId}}'">{{userId}}</span>
      <span class="user-text-box click-box"
            onclick="location.href='/users/{{userId}}'">{{email}}</span>
      <span class="user-text-box click-box"
            onclick="location.href='/users/{{userId}}'">{{nickname}}</span>
    </div>
  {{/users}}
</div>
</body>

<style>
  .users-container {
    max-width: 1048px;
    margin: 0 auto;
  }

  .users-title-box {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    padding: 40px;
    background-color: #FFCD00;
  }

  .users-title {
    color: #000000;
    font-size: 16px;
    font-weight: 500;
  }

  .user-row {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    line-height: 55px;
  }

  .user-text-box {
    height: 32px;
    min-width: 230px;
    text-align: center;
  }

  .click-box {
    cursor: pointer;
  }
</style>