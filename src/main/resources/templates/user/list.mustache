<!DOCTYPE html>
<html lang="kr">

{{>layout/header}}

<div id="main">
    <div style="display: flex; flex-direction: column; align-items: center">
        <div style="padding: 40px; background-color: #FFCD00; width: 100%; height: 148px">
            <div style="display: flex; flex-direction: column">
                <span>멤버 리스트</span> <br/>
                <span style="font-size: 24px">참여자를 확인할 수 있습니다.</span>
            </div>
        </div>
        <span style="margin-top: 40px; margin-bottom: 16px; color: #666; align-self: start">전체 멤버 {{userListInfo.total}}명</span>
          <table class="table table-hover">
              <thead>
                <tr>
                    <th>#</th>
                    <th>사용자 아이디</th>
                    <th>이름</th>
                    <th>이메일</th>
                    <th>가입 날짜</th>
                </tr>
              </thead>
              <tbody>
                  {{#userListInfo.userList}}
                      <tr>
                          <th scope="row">{{id}}</th>
                          <td><a href="/users/{{userId}}">{{userId}}</a></td>
                          <td>{{name}}</td>
                          <td>{{email}}</td>
                          <td>{{createdAt}}</td>
                      </tr>
                  {{/userListInfo.userList}}
              </tbody>
          </table>
    </div>
</div>


{{>layout/footer}}