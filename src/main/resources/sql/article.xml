<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <entry key="article.findById">
        SELECT
        a.id as id, a.title as title, a.writer as writer, ac.body as body
        FROM
        article a
        JOIN
        article_content ac
        ON
        a.id = ac.article_id
        WHERE
        a.id = :id
    </entry>
    <entry key="article.findAll">
        SELECT
        a.id as id, a.title as title, a.writer as writer, ac.body as body
        FROM
        article a
        JOIN
        article_content ac
        ON
        a.id = ac.article_id
    </entry>
    <entry key="article.save">
        INSERT INTO
        article
        (id, title, writer)
        VALUES
        (:id, :title, :writer)
    </entry>
    <entry key="article_content.save">
        INSERT INTO
        article_content
        (body, article_id)
        VALUES
        (:body, :article_id)
    </entry>
    <entry key="article.count">
        SELECT
        COUNT(*)
        FROM
        article
    </entry>
</properties>