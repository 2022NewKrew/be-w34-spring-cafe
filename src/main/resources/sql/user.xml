<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <entry key="user.findAll">
        SELECT
            id, user_id, password, name, email
        FROM
            user
    </entry>
    <entry key="user.findByUserId">
        SELECT
            id, user_id, password, name, email
        FROM
            user
        WHERE
            user_id = :user_id
    </entry>
    <entry key="user.save">
        INSERT INTO
            user
            (user_id, password, name, email)
        VALUES
            (:user_id, :password, :name, :email)
    </entry>
</properties>